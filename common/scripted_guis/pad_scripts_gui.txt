pad_update_candidates = {
	effect = {
		#clear_global_variable_list = global_var_list:pad_candidates

		every_living_character = {
			limit = {
				is_ruler = yes
			}

			add_to_global_variable_list = {
				name = pad_candidates
				target = this
			}
		}
	}
}